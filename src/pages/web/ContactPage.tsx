/**
 * Placeholder for `/contact` (`nav.contact`). Reserved here — file and route
 * both — so `routes.tsx` never needs touching again; a later track fills
 * this in with the real contact page (live details already reach the footer
 * through `fetchSiteSettings()`, `src/api/site.ts`).
 */
export function ContactPage() {
  return (
    <div data-testid="contact-page">
      <h1>Aloqa</h1>
    </div>
  );
}
