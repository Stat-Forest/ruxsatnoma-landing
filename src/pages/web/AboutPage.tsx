/**
 * Placeholder for `/about` (`nav.about`). `/faq` also redirects here — the
 * standalone FAQ screen is retired, and its content is expected to land on
 * this page. Reserved here — file and route both — so `routes.tsx` never
 * needs touching again; a later track fills this in for real.
 */
export function AboutPage() {
  return (
    <div data-testid="about-page">
      <h1>Portal haqida</h1>
    </div>
  );
}
