/**
 * Placeholder for the vacant-contours map (`/map`, `footer.link.map`).
 * Reserved here — file and route both — so `routes.tsx` never needs touching
 * again for it; a later track fills this in with the real GIS view (`3.6b`,
 * not yet planned).
 */
export function MapPage() {
  return (
    <div data-testid="map-page">
      <h1>Xarita</h1>
    </div>
  );
}
