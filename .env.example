# Backend origin. No trailing slash.
VITE_API_BASE_URL=http://localhost:8000

# The cabinet (adminka) origin. Every sign-in link on this site points here;
# the adminka decides whether the visitor sees the dashboard or the login
# page. Required — the build has no sensible default for it.
VITE_ADMIN_BASE_URL=http://localhost:5173
